CREATE FILTER MODULE CHECK_EOD_SUBFLOW_FILTER_IS_EOD
	CREATE FUNCTION Main() RETURNS BOOLEAN
	BEGIN
		IF (LocalEnvironment.EOD_CONFIG.EOD_STATUS IS NOT NULL) AND (LocalEnvironment.EOD_CONFIG.EOD_STATUS = '0') THEN
			RETURN FALSE;
		ELSE
			RETURN TRUE;
		END IF;
	END;
END MODULE;
